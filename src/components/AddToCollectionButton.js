import React, { Component } from 'react'
import { withAuth } from '../lib/Auth'

class AddToCollectionButton extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default withAuth(AddToCollectionButton)